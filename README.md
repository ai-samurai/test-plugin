# godot cpp tileMap plugin
This repository is for creating a C++ plugin using GDExtensions. 
The plugin will be used to procedurally generate a tileMap. 
The tiles used will be pre-existing images. 
The orientation and placement of the tiles will be handled by the plugin.
